version: '3.8'

services:
  plant-care-frontend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # Si necesitas conectar con tu backend Django, descomenta y ajusta:
    # depends_on:
    #   - django-backend
    # networks:
    #   - plant-care-network

# networks:
#   plant-care-network:
#     driver: bridge
